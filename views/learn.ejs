<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Säljutbildning — Joakim Jaksen</title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🎯</text></svg>" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body class="learn-page">

  <nav class="learn-nav">
    <a href="/dashboard" class="learn-nav-back">← Dashboard</a>
    <span class="learn-nav-title">Säljutbildning</span>
    <span class="learn-nav-user"><%= username %></span>
  </nav>

  <div class="learn-hero">
    <h1>Joakim Jaksens Säljutbildning</h1>
    <p>16 block — från första intrycket till mental styrka. Teori, video och prov per block.</p>
    <% if (role === 'free') { %>
      <a href="/upgrade" class="learn-upgrade-cta">⭐ Lås upp alla 16 block med Premium →</a>
    <% } %>
  </div>

  <div class="blocks-grid">
    <% blocks.forEach(function(block, i) { %>
      <% const isLocked = role === 'free' && !freeBlockIds.includes(block.id); %>
      <a href="/learn/<%= block.id %>"
         class="block-card <%= isLocked ? 'block-locked' : '' %>"
         style="--card-gradient: <%= block.gradient %>;">
        <div class="block-card-number"><%= i + 1 %></div>
        <div class="block-card-icon"><%= isLocked ? '🔒' : block.icon %></div>
        <div class="block-card-body">
          <h3><%= block.title %></h3>
          <p><%= block.subtitle %></p>
        </div>
        <div class="block-card-arrow"><%= isLocked ? '👁' : '→' %></div>
      </a>
    <% }); %>
  </div>

</body>
</html>
